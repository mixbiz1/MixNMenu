# MXMN CURRENT STATUS

**Version:** 1.13\
**Status date:** 2026-09-17\
**Project:** MXMN\
**Purpose:** 현재 실제 구현·검증 상태와 다음 작업을 짧게 유지하는 운영
문서

------------------------------------------------------------------------

## 1. 현재 개발 위치

MXMN은 MASTER 영역의 **거래처관리·공통코드관리·상품공통코드관리·
계층형 상품분류·상품 Master 입력 Vertical Slice를 구현하고 주요 사용자
실행검증을 완료**하였다.

상품코드·창고 Master·LOT 조회/보정 영역은 **1차 완료**하였다. 다음 개발
단계는 시스템 도입 시점의 최초재고와 거래처 최초잔액을 등록하는
`초기자료등록` Vertical Slice이다.

기본 실행 구조는 다음과 같다.

`PySide6 Client → HTTP → FastAPI → SQLAlchemy / pyodbc → MS SQL Server (mxmn_dev)`

현재 개발은 기존 정상 기능을 유지하면서 작은 기능 단위로 수정하고, 실제
실행 검증 후 Git Commit하는 방식으로 진행한다.

------------------------------------------------------------------------

## 2. 현재까지 정상 작동 확인

### 2.1 개발환경 / DB / API

-   Windows + Python 3.11+ 가상환경 `.venv`
-   MS SQL Server 2025 Express / DB `mxmn_dev`
-   SQLAlchemy + pyodbc DB 연결
-   FastAPI 서버 실행
-   PySide6 Desktop Client 실행
-   로그인 → Main Window 진입
-   업무회사 선택 및 변경
-   Git / GitHub 기반 소스 버전관리

### 2.2 FastAPI 시작 지연 조치

기존 `main.py`에서 서버 시작 시 실행되던 아래 코드는 현재 주석
처리하였다.

`Base.metadata.create_all(bind=engine)`

DB 구조는 Migration으로 관리하므로 FastAPI 실행 때마다 전체 Metadata를
확인/생성하지 않는 방향으로 전환하였다. 이 조치 후 FastAPI 시작 지연이
크게 줄었다.

### 2.3 SQL Server 일시 응답불능 문제

2026-09-15 개발 중 다음 현상이 발생하였다.

-   1433 Port는 `TcpTestSucceeded : True`
-   FastAPI 자체는 정상 기동
-   pyodbc 직접연결은 Login Timeout
-   SSMS에서도 `127.0.0.1,1433` 접속 Timeout
-   `MSSQL$SQLEXPRESS` 서비스는 Running으로 표시

`MSSQL$SQLEXPRESS` 서비스를 재시작한 후 SSMS 및 프로그램 연결이
정상화되었다.

따라서 동일 증상 발생 시 **Python/FastAPI 코드를 먼저 수정하지 말고 SQL
Server 실제 응답상태를 먼저 점검**한다.

------------------------------------------------------------------------

## 3. 거래처관리 현재 구현 상태

### 3.1 거래처 Master

현재 거래처 공통 Master와 업무회사별 거래처 관계를 분리하여 관리한다.

주요 구현/확인 사항:

-   거래처 조회
-   신규 등록
-   수정 / 저장
-   거래처코드 자동발번
-   거래처명 필수
-   사업자번호
-   법인번호
-   대표자
-   우편번호 / 주소
-   업태 / 업종
-   전화 / 팩스
-   담당자
-   전자(세금)계산서 이메일
-   계좌정보
-   공통 메모
-   Enter 중심 입력항목 이동

### 3.2 거래처 화면 간소화

실무 검토 결과 다음 방향으로 정리하였다.

-   좌측 거래처 목록은 **거래처명을 가장 먼저, 충분한 폭으로 표시**
-   담당자 휴대폰: 화면에서 제거
-   은행명 / 계좌번호 / 예금주: **계좌정보 한 칸으로 통합**
-   축산물이력제 거래처번호: 현재 화면에서 제거
-   상세주소 별도 입력: 제거하고 주소 한 칸으로 사용
-   전자(세금)계산서 이메일: 한 개만 사용
-   기존 DB 컬럼은 불필요하게 DROP하지 않고 호환성을 위해 유지 가능

### 3.3 업무회사별 거래정보

거래처 공통정보와 별도로 회사별 관계정보를 관리한다.

현재 주요 항목:

-   거래상태: 거래처 / 정보등록만
-   업무유형
    -   일반
    -   계약
    -   유통
    -   BL양수도
    -   보증금
    -   기타
-   매입거래 여부
-   매출거래 여부
-   (세금)계산서 발행대상 여부
-   거래중단 여부

`거래중단`은 단순한 정보등록 여부와 다른 개념이다. 거래중단된 업체는
일반 매입/매출 거래처 조회에서 기본적으로 보이지 않게 하고, 필요 시
`거래중단 포함`으로 조회한다.

------------------------------------------------------------------------

## 4. 거래처명 사용에 관한 실무 기준

동일 법적 사업자라도 업무상 역할을 구분하기 위해 복수의 ERP 거래처코드를
사용할 수 있다.

예:

-   `(계약)(주)제이케이미트코퍼레이션`
-   `(유통)(주)제이케이미트코퍼레이션`
-   `(BL양수도)(주)제이케이미트코퍼레이션`
-   `(보증금)(주)제이케이미트코퍼레이션`

이는 장기 목표인 **법적 사업자(Partner)와 업무 거래처코드(Customer)의
분리**와 연결되는 실무 요구사항이다.

별도의 `계약서 거래처명` 필드를 중복 관리하기보다는 실제 거래처명과 향후
Partner의 법적 상호를 적절히 분리하는 방향으로 발전시킨다.

------------------------------------------------------------------------

## 5. (세금)계산서 발행 설계 원칙

거래처 화면에서는 단순하고 직관적으로 다음 설정을 사용한다.

`□ (세금)계산서 발행대상`

단, 실제 계산서 발행은 거래처 설정만으로 판단하지 않는다.

### 중복발행 방지 원칙

월중 여러 건의 거래명세서를 발행하는 거래처가 특정 거래에 대해
거래명세서와 함께 단건 계산서 발행을 요청할 수 있다.

따라서 월말 합산발행 시:

1.  해당 월의 계산서 발행대상 매출을 조회
2.  이미 단건 계산서에 포함된 원거래를 확인
3.  단건 발행 완료 거래는 월합산 대상에서 제외
4.  남은 미발행 거래만 합산
5.  월합산 계산서 생성
6.  계산서와 포함된 원거래 간 연결관계를 저장

**중복방지는 금액 차감 방식이 아니라 원거래 ID와 계산서 발행이력의
연결관계로 관리한다.**

향후 `tb_tax_document` 및 관련 Detail/Link 구조 설계 시 이 원칙을
반영한다.

------------------------------------------------------------------------

## 6. 최근 DB Migration

거래처관리 과정에서 `tb_account` 및 `tb_company_account`를 단계적으로
확장하였다.

최근 주요 추가사항:

### tb_account

-   `tax_email`
-   계좌 관련 정보
-   주소/사업자 관련 보완정보
-   memo 등

### tb_company_account

-   `trade_status`
-   `trade_type`
-   매입/매출 관련 구분
-   계산서 관련 설정
-   `trade_stop_yn`
-   `invoice_issue_yn`

Migration 원칙:

-   기존 데이터 DELETE 없음
-   기존 Table DROP 없음
-   기존 PK 변경 없음
-   기존 FK 임의 제거 없음
-   필요한 컬럼을 단계적으로 추가

------------------------------------------------------------------------

## 7. 최근 해결한 주요 오류

### 거래처 화면

-   단축메뉴 `코드관리 → 거래처입력` 연결
-   거래처입력 창을 닫은 후 다시 열리지 않던 문제
-   신규 거래처코드 자동발번 문제
-   업무회사 변경이 반응하지 않던 문제
-   거래처 수정/저장 API 오류
-   `trade_stop_yn`, `invoice_issue_yn` DB 컬럼 불일치에 따른 500 오류

### 서버 / DB

-   FastAPI 시작 지연
-   SQL Server Login Timeout
-   SQLEXPRESS 서비스 Running 상태의 실제 응답불능

현재 위 문제들은 조치 후 정상 작동을 확인하였다.

------------------------------------------------------------------------

## 8. UI / Theme 현재 상태

거래처입력 화면의 Light/Dark Mode 체크박스 가독성 개선을 완료하였다.

적용 대상:

-   매입거래
-   매출거래
-   (세금)계산서 발행대상
-   거래중단
-   거래중단 포함

적용 내용:

-   미체크 `□`, 체크 `☑` 상태기호 적용
-   체크 상태의 배경색·글자색·테두리 대비 강화
-   Light/Dark Mode별 Hover·Focus 상태 구분
-   기존 `isChecked()` 저장·조회 바인딩 및 거래처 CRUD 기능 유지
-   공통 확장이 가능한 `ReadableCheckBox` 구조 적용

2026-09-16 사용자 실행 확인 결과 체크박스 개선이 정상 반영되었다.

### 8.1 화면 초기조회 및 사용자 반응 원칙

코드관리·거래처입력·상품공통코드·상품입력 화면은 초기 API/DB 조회가
완료될 때까지 창 표시가 늦어지는 구조를 개선하였다.

2026-09-16 사용자 실행검증 결과:

-   메뉴 클릭 후 화면 포맷이 먼저 표시됨
-   데이터가 뒤이어 채워져 프로그램이 반응 중임을 확인할 수 있음
-   거래처 및 상품공통코드의 독립 API 요청 병렬화 적용
-   기존 기능 오류 없음

앞으로 데이터 조회가 있는 모든 화면에는 다음 원칙을 공통 적용한다.

1.  메뉴 클릭 시 화면을 먼저 표시한다.
2.  초기 데이터는 화면 표시 이후 조회한다.
3.  독립적인 API 요청은 안전한 범위에서 병렬 처리한다.
4.  조회 중에는 버튼을 `조회 중...`으로 표시하고 중복 입력을 차단한다.
5.  완료 또는 오류 발생 시 버튼과 입력 상태를 반드시 복원한다.
6.  장시간 조회는 공통 Background Worker/QThread 구조로 단계적으로 전환한다.
7.  조회·저장 로직과 화면 갱신은 분리하여 UI 스레드 안전성을 유지한다.

2차 개선으로 코드관리·거래처입력·상품공통코드·상품입력 조회 버튼의
`조회 중...` 상태 표시 및 중복 실행 차단을 적용하였다.

------------------------------------------------------------------------

## 9. 주소검색 현재 상태

거래처 화면에 주소찾기 기능을 1차 적용하였다.

현재는 외부 주소검색 사이트를 열어 우편번호/주소를 입력하는 보조
방식이다.

향후 목표:

`주소찾기 → 주소검색 → 주소 선택 → 우편번호/기본주소 자동입력`

완전 자동 연동은 별도 안정화 작업으로 진행한다.

------------------------------------------------------------------------

## 10. 현재 기준 파일 / 관리 원칙

실제 소스코드의 기준은 ChatGPT 대화가 아니라 다음으로 한다.

`C:\projects\MixNMenu → Git → GitHub`

ChatGPT Project의 문서는 설계 및 현재 상태 공유에 사용한다.

주요 기준 문서:

-   `00_PROJECT_BASELINE.md`
-   `01_PROJECT_SPEC.md`
-   `02_DOMAIN_MODEL.md`
-   `03_DATABASE_DESIGN.md`
-   `04_CURRENT_STATUS.md` ← **항상 최신 상태로 갱신**
-   `05_FOLDER_STRUCTURE.md`
-   `10_DEVELOPMENT_ROADMAP.md`
-   `11_CODEBASE_GAP_ANALYSIS.md`
-   `12_MIGRATION_PLAN.md`
-   `AI_SYSTEM_PROMPT.md`

------------------------------------------------------------------------

## 11. ChatGPT Project 운영 방식

하나의 초장기 대화에 모든 개발을 누적하지 않는다.

**MXMN Project 하나 + 기능별 작업 대화**를 기본으로 한다.

예:

-   `00 마스터 / 설계`
-   `01 개발환경 / 서버 / DB`
-   `02 로그인 / Main Window`
-   `03 거래처관리`
-   `04 공통코드관리`
-   `05 상품관리`
-   `06 수입 / Offer / 계약`
-   `07 입출고 / 재고`
-   `08 입금 / 출금 / 정산`
-   `09 계산서 / Report`

대화가 지나치게 길어지면 같은 기능도 새 대화로 이어간다.

새 대화에서는 과거 전체 대화를 다시 설명하지 않고 **Project 기준문서 +
04_CURRENT_STATUS.md + Git 최신 코드**를 기준으로 작업한다.

------------------------------------------------------------------------

## 12. 앞으로의 작업 절차

기본 반복 Cycle:

`요구사항/오류 → GPT 관련 파일 검토 → 수정 완료본 → 사용자 실행검증 → 정상 확인 → Git Commit → 04_CURRENT_STATUS.md 갱신 → 다음 작업`

원칙:

-   한 줄씩 반복 수정하는 방식 최소화
-   수정 전 관련 파일과 호출관계 확인
-   가능하면 전체 수정파일 제공
-   사용자는 코드 자체보다 실제 업무 적합성과 실행결과를 검증
-   정상 기능은 불필요하게 다시 수정하지 않음
-   오류 발생 시 관련 로그/파일만 확인
-   기능 완료 후 대화가 길어졌으면 새 대화로 이동

------------------------------------------------------------------------

## 13. 거래처관리 Vertical Slice 완료

2026-09-16 사용자 실행검증을 통해 다음 사항의 정상 작동을 확인하였다.

-   거래처 조회·신규·수정·저장
-   업무회사별 거래정보 저장 및 재조회
-   거래중단 기본 제외 및 거래중단 포함 조회
-   Light/Dark Mode 체크박스 가독성
-   동일 사업자번호의 복수 거래처코드 등록
-   신규등록 기본값: 거래처 / 일반 / 매입 / 매출 / 계산서 발행대상

------------------------------------------------------------------------

## 14. 공통코드·상품공통코드 현재 상태

### 14.1 사용자 실행검증 완료

2026-09-16 사용자 실행검증을 통해 다음 사항이 정상 작동함을 확인하였다.

-   일반 공통코드 그룹·상세코드 신규/수정/저장
-   그룹코드 `CG001` 형식 및 그룹별 상세코드 `001` 형식 자동발번
-   상세코드 오름차순/내림차순 정렬
-   일반 공통코드 검색 및 새로고침
-   상품공통코드 기본 12개 분류의 DB 그룹 연계
-   상품분류 `PC001` 형식 자동발번
-   상품분류 신규/수정/사용중지 및 복구
-   상품 상세코드 신규/수정/사용중지
-   분류·코드 삭제 요청 시 2회 확인
-   물리삭제 대신 사용중지하여 기존 참조자료 보존
-   거래처·일반 공통코드·상품공통코드 새로고침
-   Light/Dark Mode에서 가독성 높은 `□/☑` 체크박스

### 14.2 통합 검색·화면전환 검증 완료

다음 기능은 사용자 실행검증을 완료하였다.

-   공통코드와 상품공통코드 조회방식 통일
-   현재 선택 분류 안에서 키워드 검색
-   검색 결과 건수 또는 검색 결과 없음 표시
-   검색 후 왼쪽 분류를 클릭하면 검색어 해제 및 해당 분류 전체 표시
-   가장 최근 명령한 MDI 창을 맨 앞으로 표시
-   선택행 글자색을 Light/Dark Mode에서 명확하게 표시

### 14.3 삭제 정책

-   저장된 Master/코드는 원칙적으로 물리삭제하지 않는다.
-   삭제 버튼은 2회 확인 후 `use_yn = False`로 사용중지한다.
-   `사용중지 포함`으로 재조회한 뒤 `사용` 체크 및 저장으로 복구한다.
-   거래처는 삭제 대신 기존 `거래중단` 상태로 관리한다.

------------------------------------------------------------------------

## 15. 다음 작업

### 15.1 상품코드 영역 완료사항

-   대분류 → 중분류 → 세부부위 계층형 상품분류
-   내부 상품·분류코드 자동발번 및 화면 코드 노출 최소화
-   세부부위 선택 후 상품 등록 및 첫 상품 즉시 목록 반영
-   브랜드·등급·부위·원산지·공장번호·포장·기타 상품명 실시간 조합
-   조합 결과의 상품명 직접 수정 및 독립 저장
-   분류 없이 단독 상품 등록
-   조회·신규·수정·저장·취소·사용중지·복구
-   F2~F7 단축키와 Enter/Tab 입력 이동
-   Light/Dark Mode 선택행 및 체크박스 가독성
-   화면 선표시, 조회상태 표시, 중복조회 방지
-   상품목록 공통코드 조합 일괄조회로 N+1 쿼리 제거
-   공통코드 선택값의 기본 이름 오름차순(가나다/ABC) 정렬
-   선택창 오른쪽 `⇅` 버튼으로 이름·코드 오름/내림차순 전환
-   정렬 변경 시 기존 선택값과 상품명 조합 유지

### 15.2 다음 우선순위

1.  LOT 업무범위·필수 추적항목 확정
2.  LOT 모델/API/Migration/입력화면 Vertical Slice 구현
3.  LOT → 입고 → 재고 → 출고 흐름을 작은 기능 단위로 연결

데이터가 크게 증가한 이후 필요한 서버 페이징과 공통 QThread Worker는
현재 기능 완료의 차단사항이 아니며 전역 성능개선 과제로 관리한다.

------------------------------------------------------------------------

## 16. Git 기준점

2026-09-17 LOT 1차 마무리 기준:

-   저장소: `mixbiz1/MixNMenu`
-   Branch: `main`
-   최신 기능 Commit: `3f7687f0e85e59959a7c6c9db3380f121c769550`
-   Commit Message: `fix: enforce lot numeric precision rules`
-   LOT 기능·정밀도 원칙은 위 Commit을 정상 기준점으로 한다.

향후 이 문서에는 필요 시 다음 정보를 기록한다.

-   최신 정상 Commit Hash
-   Commit Message
-   현재 Branch
-   미Commit 변경사항 여부

**코드의 최종 진실(Source of Truth)은 Git/GitHub이며, 이 문서는 현재
개발상태를 빠르게 인수인계하기 위한 상태표이다.**

------------------------------------------------------------------------

## 17. 다른 PC에서 재개 절차

```bat
cd C:\projects\MixNMenu
git pull origin main
venv\Scripts\activate
```

1.  FastAPI 서버와 MXMN을 실행한다.
2.  FastAPI와 MXMN Client의 정상 실행을 확인한다.
3.  `04_CURRENT_STATUS.md`와 GitHub `main` 최신 소스를 기준으로 작업한다.
4.  이 문서 마지막의 새 대화 시작문으로 초기자료등록 설계를 시작한다.

LOT 정밀도 최종 보완에는 DB 구조 변경이 없으므로 별도 Migration은
필요하지 않다. FastAPI와 MXMN Client는 재시작한다.

------------------------------------------------------------------------

## 18. 2026-09-16 작업 종료 요약

오늘 완료하고 사용자 실행검증한 주요 내용:

1.  거래처입력 체크박스 Light/Dark Mode 가독성 및 신규 기본값 정리
2.  동일 사업자번호의 복수 거래처코드 등록 허용
3.  일반 공통코드의 자동발번·검색·정렬·새로고침 개선
4.  상품공통코드 12개 분류의 DB 그룹 연계 및 관리 기능 구현
5.  계층형 상품분류와 세부부위 추가·수정·사용중지 구현
6.  상품 Master 신규·수정·사용중지·복구 및 내부코드 자동발번
7.  브랜드+등급+부위+원산지+공장번호+포장+기타 상품명 실시간 조합
8.  최초 상품 저장 후 목록 미표시 오류 수정 및 규격 입력란 제거
9.  메뉴 클릭 후 화면 우선 표시, 독립 API 병렬조회, `조회 중...` 표시
10. 코드관리·상품공통코드·상품입력 선택행 글자색 대비 개선
11. 상품목록 공통코드 N+1 조회 제거
12. 동적 코드 선택창의 이름순 기본정렬 및 `⇅` 정렬 메뉴 구현

상품코드 영역은 **1차 기능 완료**로 종료한다. 이후 실제 입출고 개발에서
추가 필드나 연계 요구가 확인되면 기존 정상 기능을 유지하면서 확장한다.

창고 Master 착수용 과거 인계문은 완료되어 제거하였다. 현재 유효한 새 대화
시작문은 이 문서 마지막의 LOT 착수용 인계문을 사용한다.

------------------------------------------------------------------------

## 19. 창고 Master 1차 구현 (2026-09-17)

### 설계 및 구현

-   실제 창고 공통정보: `tb_warehouse`
-   업무회사별 창고 사용관계: `tb_company_warehouse`
-   회사별 요율 적용기간 이력: `tb_warehouse_rate`
-   창고코드 `W0001` 형식 자동발번
-   창고명, 일반/보세, 냉동/냉장/상온/혼합
-   사업자번호, 주소, 전화, 담당자, 축산물이력제 사업장번호
-   입고료·출고료(원/kg), 보관료(원/kg·일), 계근료(원/box)
-   적용 시작일·종료일 및 부가세 별도 여부
-   현재 업무회사에서 사용/사용중지
-   코드관리 메뉴 `6.창고입력` 연결
-   조회 중 표시 및 중복조회 방지

### 변경 파일

-   `models.py`
-   `main.py`
-   `db_warehouse_migrate.py`
-   `views/warehouse_reg.py`
-   `views/mxmn_main_window.py`
-   관련 `docs/` 기준문서

### 검증 상태

-   Python 문법검사 및 `git diff --check` 통과
-   PySide6 실제 import 및 Offscreen 화면 생성검증 통과
-   사용자 PC SQL Server Migration 및 창고 CRUD 실행검증 완료
-   조회·신규·수정·저장·사용중지·닫기 후 재실행 정상
-   기존 상품·거래처·공통코드 기능 변경 없음

### 19.1 사용자 1차 실행검토 후 보완

2026-09-17 창고입력 화면 실행검토를 반영하여 다음을 수정하였다.

-   Tab뿐 아니라 Enter로 다음 입력항목 이동
-   신규 창고의 기본 창고구분을 보세창고로 변경
-   전화 외 팩스, 입출고지시 웹주소, 웹 사용자 ID 추가
-   고정된 네 개 요율 입력칸을 가변 비용항목 표로 변경
-   창고별 비용항목 추가·선택삭제
-   계산기준: KG당 / BOX당 / KG·일당 / 건당 정액
-   비용항목명 중복 및 빈 항목 검증
-   `tb_warehouse_charge` 추가
-   기존 고정요율 중 0이 아닌 값은 Migration 시 새 비용항목으로 승계

기존 `tb_warehouse_rate`의 네 개 고정요율 컬럼은 기존 데이터 호환을 위해
삭제하지 않는다. 신규 화면/API는 `tb_warehouse_charge`를 기준으로 사용한다.

### 19.2 사용자 2차 실행검토 후 보완

-   보관유형 신규 기본값을 `혼합`으로 변경
-   입출고 웹주소와 웹 사용자 ID를 같은 줄로 배치
-   웹 사용자 ID 입력칸 앞에 항상 보이는 `ID` 항목명 표시
-   웹주소 옆 `웹주소 이동` 버튼 추가
-   신규 입력 시 기본 비용항목을 보관비(KG·일당)·입출고비(KG당)·
    상하차비(BOX당)·계근비(BOX당)로 구성
-   조회영역에 `조회 [F7]`·새로고침·`신규 [F2]`·`저장 [F4]`·
    `사용중지 [F6]`·닫기를 동일 순서로 배치
-   웹 입력영역 한 줄 축소로 한 화면 표시공간 확보
-   닫기 버튼이 내부 위젯만 숨겨 재실행되지 않던 MDI 오류 수정
-   기존 창 감지 시 내부 위젯을 다시 표시하는 방어처리 추가

창고입력 화면을 닫은 후 코드관리 → 창고입력을 다시 선택하는 동작까지
사용자 실행검증을 완료하였다.

### 19.3 검증환경 및 상하차비 기준 보완

-   상하차비 기본 계산기준을 `BOX당`으로 확정
-   Python 검증 전 PySide6·SQLAlchemy·pyodbc·FastAPI·Pydantic·httpx·
    python-dotenv 설치 원칙을 `AI_SYSTEM_PROMPT.md`에 추가
-   PySide6 실제 import 및 Offscreen 창고입력 화면 생성 성공
-   신규 기본값 `보세창고 + 혼합` 확인
-   기본 비용항목과 계산기준 실제 객체값 확인
-   현재 Linux 검증환경은 시스템 ODBC 런타임 `libodbc.so.2`가 없어
    pyodbc 실제 import/DB 연결검증은 불가하며, 사용자 Windows 환경에서
    최종 DB 실행검증을 진행한다.

### 19.4 창고 Master 1차 완료

-   `웹주소 이동` 클릭 시 창고 사이트를 열고 저장된 웹 사용자 ID를
    클립보드에 함께 복사
-   ID가 없으면 사이트만 이동
-   비밀번호는 MXMN에 저장하지 않음
-   사용자 Windows 환경에서 전체 창고입력 기능 정상 작동 확인

창고 Master는 **1차 완료**로 종료한다. 실제 출고업무 개발 시 창고별 웹
출고지시 연동·업로드 양식·브라우저 자동화 필요성을 다시 검토한다.

### 다음 새 대화 시작문

> MXMN 개발을 GitHub `mixbiz1/MixNMenu`의 `main` 최신 소스와
> `docs/04_CURRENT_STATUS.md` 기준으로 이어서 진행합니다. 상품코드와 창고
> Master는 1차 완료되었습니다. 다음 단계로 LOT Master를 먼저 설계·
> 구현하고, 이후 입고 → 재고 → 출고 Vertical Slice로 연결해 주세요.
> 기존 정상 기능은 변경하지 말고 관련 기준문서·레거시 DB·모델·API·
> Migration·메뉴 구조를 먼저 확인한 뒤 작은 기능 단위로 수정·검증·
> GitHub 동기화해 주세요.

------------------------------------------------------------------------

## 21. 초기자료등록 Vertical Slice 1차 구현 (2026-09-17)

### 21.1 메뉴와 화면

- 코드관리의 기존 단일 `거래처 기초잔액 입력` 자리에는
  `8.초기자료등록` 상위 메뉴를 배치하였다.
- 하위 메뉴는 `1.최초재고 등록`, `2.거래처 최초잔액 등록`이다.
- 최초재고 화면은 회사·기준일·창고 Header와 복수 Detail 행을 입력한다.
- Detail은 상품·수입/국내매입·공급자 LOT·BL·컨테이너·이력번호·원산지·
  EST·생산일·소비기한·BOX·KG·개별원가·메모를 지원한다.
- 거래처 최초잔액 화면은 기준일·미수/미지급·거래처·금액·메모를 입력하고,
  이미 등록한 원거래의 최초금액·배분금액·미결잔액을 조회한다.

### 21.2 원장과 원자성

- `tb_inbound`, `tb_inbound_item`을 추가하였다.
- 최초재고 저장 시 입고 Header, 각 LOT, 각 입고 Detail을 단일 DB
  Transaction에서 생성하며 오류 시 전체 Rollback한다.
- 재고수량은 LOT Master에 저장하지 않고 입고 Detail의 BOX·KG를 원본으로
  사용한다.
- `tb_account_transaction`에 최초 미수금과 최초 미지급금을 각각 독립된
  원거래로 생성한다.
- `tb_account_transaction_allocation`은 향후 수금·지급 Transaction과
  원거래를 연결하여 부분수금·부분지급을 지원한다.
- 미결잔액은 원거래 금액에서 Allocation 합계를 차감하여 조회한다.

### 21.3 수치 원칙

- 가격·단가·개별원가·금액: 원 단위 정수, 입력 소수점은 올림
- 중량: KG 소수점 둘째 자리
- BOX: 정수
- 환율: 소수점 둘째 자리(이번 화면에는 환율 입력 없음)
- 기존 LOT 원가 컬럼의 Scale은 호환성을 위해 유지하고 API 저장 규칙으로
  정수 원을 보장한다.

### 21.4 변경 파일

- `models.py`
- `main.py`
- `db_opening_data_migrate.py`
- `views/opening_inventory_reg.py`
- `views/opening_balance_reg.py`
- `views/mxmn_main_window.py`
- `docs/03_DATABASE_DESIGN.md`
- `docs/04_CURRENT_STATUS.md`
- `docs/05_FOLDER_STRUCTURE.md`
- `docs/12_MIGRATION_PLAN.md`

### 21.5 검증 상태와 사용자 PC 실행 순서

- Python 문법검사 통과
- 임시 SQLite 기반 FastAPI 통합검증 통과
  - 최초재고 저장 시 입고·LOT·Detail 동시 생성
  - 잘못된 상품 입력 시 원장·LOT 추가 없음
  - 중량 둘째 자리와 원 단위 올림 확인
  - 미수금·미지급금 원거래 및 미결잔액 확인
- PySide6 Offscreen 두 화면 생성검증 통과
- `git diff --check` 통과
- 현재 Linux 환경은 `libodbc.so.2`가 없어 SQL Server 연결검증 불가
- 사용자 Windows PC에서는 먼저 `python db_opening_data_migrate.py`를 실행한
  뒤 FastAPI와 Client를 재시작하여 실제 저장을 검증한다.

### 다음 확인 순서

1. 최초재고 1건 저장 후 LOT조회/보정에서 생성 LOT 확인
2. SQL 또는 후속 재고조회에서 입고 BOX·KG 확인
3. 매출거래 거래처의 최초 미수금 등록
4. 매입거래 거래처의 최초 미지급금 등록
5. 등록목록의 최초금액·배분금액 0·미결잔액 일치 확인
6. 다음 Vertical Slice에서 재고조회와 수금·지급 Allocation 구현

------------------------------------------------------------------------

## 22. 초기자료등록 사용자 실행검토 보완 (2026-09-17)

### 최초재고

- 기준일·생산일은 달력 선택과 수기입력을 함께 지원한다.
- Enter 키로 Header와 Detail의 다음 입력항목으로 이동한다.
- 상품명 열의 과도한 폭을 줄이고 LOT·BL·컨테이너·날짜·수량·금액 열을
  실무 입력 가능한 고정 폭으로 조정하였다.
- 원산지와 EST는 상품 Master에 있으므로 최초재고 입력열에서 제거하고 LOT에
  자동 Snapshot한다.
- 생산일만 입력하며 소비기한은 상품별 규칙으로 자동계산하여 읽기 전용
  표시한다.
- 상품 Master에 자동판단·냉동 2년-1일·지정일수-1일·계산 안 함 규칙을
  추가하였다.
- `KG × 개별원가`를 원 단위 올림하여 재고금액으로 즉시 표시한다.
- 저장된 최초재고 목록에 전표번호·LOT번호·BOX·KG·원가·금액·소비기한을
  표시한다.
- 저장목록 선택 후 수정·삭제가 가능하다. 후속 입출고에 연결된 LOT는 DB
  제약과 API에서 수정·삭제를 차단한다.

### 거래처 최초잔액

- 기준일은 달력 선택과 `2026-9-17` 형식 수기입력을 지원한다.
- Enter 키로 기준일 → 구분 → 거래처 → 금액 → 메모 → 저장 순서로 이동한다.
- 좌측 입력영역과 거래처 선택 폭을 줄이고 우측 목록의 거래처·원거래번호·
  최초금액·배분금액·미결잔액 열에 고정 폭을 배분하였다.
- 저장목록 선택 후 원거래 수정·삭제를 지원한다.
- 수금·지급 Allocation이 존재하는 원거래는 수정·삭제하지 못한다.

### Migration 및 검증

- 기존 `db_opening_data_migrate.py`를 다시 실행하면 상품 소비기한 규칙 컬럼만
  비파괴 방식으로 추가된다. 초기자료 원장 테이블은 중복 생성하지 않는다.
- Python 문법검사, PySide6 Offscreen 화면 생성, SQLite API 통합검증 통과
- 최초재고 생성·목록 응답·LOT 자동발번·원산지/EST 승계·소비기한 계산·
  수정·삭제와 거래처 최초잔액 수정·삭제 검증 통과

------------------------------------------------------------------------

## 23. LOT 식별·상품단가·코드관리 메뉴 보완 (2026-09-17)

- 신규 최초재고 LOT번호는 `L기준일-최초입고 Header 순번-Detail 생성순번`
  형식으로 생성한다. 예: `L20260917-001-02`.
- 기존 LOT번호는 수정 시에도 유지하여 후속 입출고 연결과 추적성을 보존한다.
- 공급자 LOT는 공급자가 포장·라벨에 표시한 외부 추적번호이며, 화면 열 이름을
  `공급자 LOT(포장표기)`로 명확히 하였다.
- 상품 Master 기본단가는 정수 원 단위로 입력·표시하며 천 단위 구분기호를
  표시한다. API는 소수 입력이 들어와도 원 단위로 올림한다.
- 상위 코드관리 메뉴의 `2.코드관리`를 `2.공통코드입력`으로 변경하고,
  바탕화면의 `공통코드입력`을 같은 화면에 연결하였다.
- 바탕화면의 `상품코드입력`은 상위 메뉴 `3.상품공통코드`와 같은 화면에,
  `상품입력코드`는 기존 상품 Master 입력 화면에 연결하였다.
- 사용하지 않는 `9.더존iU연동관리` 메뉴를 제거하였다.

------------------------------------------------------------------------

## 24. 금액 표시·가변 열 폭 원칙 보완 (2026-09-17)

- 모든 가격·단가·원가·금액 화면값은 정수 원 단위와 천 단위 쉼표를 기본
  표시원칙으로 한다. 계산·API 전송 시에는 쉼표를 제거한 숫자를 사용한다.
- 최초재고 개별원가는 편집 완료 시 `6,000` 형식으로 자동 표시한다.
- 날짜·구분·BOX·KG처럼 길이가 예측되는 열은 좁은 최소·최대 폭을 적용한다.
- 상품명·LOT·BL·컨테이너·메모처럼 길이가 달라지는 열은 실제 내용에 따라
  최소·최대 폭 안에서 자동 확대·축소하고 사용자가 수동 조정할 수도 있다.
- 최대 폭보다 긴 값은 셀 Tooltip으로 전체 내용을 확인할 수 있도록 한다.
- 최초재고 BL 입력열을 확대하고 저장목록의 상품명 과대 확장을 제거하였다.
- 바탕화면 `상품코드입력` 명칭을 `상품공통코드입력`으로 변경하고 기존
  상품공통코드 화면 연결은 유지하였다.

------------------------------------------------------------------------

## 25. LOT 소비기한 자동계산·목록 정렬 원칙 (2026-09-17)

- LOT 조회/보정에서 상품 또는 생산일을 변경하면 상품 Master의 소비기한
  규칙을 즉시 다시 적용한다.
- `AUTO` 상품은 냉장으로 명시된 경우를 제외하고 일반 수입 냉동육 기본값인
  생산일 + 2년 - 1일을 적용한다.
- 냉장육·특정 브랜드는 상품 Master의 `DAYS` 또는 `NONE` 규칙으로 예외를
  명시한다.
- 자동계산된 소비기한은 특별한 사유가 있으면 LOT 화면에서 수동 보정할 수
  있으며, 단순 조회만으로 기존 수동 보정값을 덮어쓰지 않는다.
- LOT 목록은 열 제목 클릭 시 오름차순·내림차순을 전환한다. 정렬 후에도
  화면행이 아닌 LOT ID를 기준으로 선택·수정한다.
- 앞으로 신규 또는 개선하는 조회목록은 제목 클릭 정렬을 기본원칙으로 하며,
  날짜·숫자·금액은 표시문자가 아닌 실제 값 기준으로 정렬한다.

### LOT 착수 시 우선 확인사항

-   LOT 자동발번과 업무상 표시번호
-   업무회사·창고·상품 연결
-   BL번호·컨테이너번호·축산물이력번호
-   원산지·EST·생산일·소비기한
-   입고 박스·중량과 LOT별 개별원가
-   동일 BL의 복수 컨테이너 및 부분 입출고
-   수입 LOT와 국내매입 LOT 공통/차이
-   LOT 상태와 사용중지·마감 정책

------------------------------------------------------------------------

## 20. LOT Master 1차 완료 (2026-09-17)

### 20.1 설계 확정

-   LOT는 상품의 입고분을 식별하는 추적 Master이며 재고수량 장부가 아니다.
-   회사별 업무 표시번호는 `LYYYYMMDD-001` 형식으로 자동발번한다.
-   수입 LOT와 국내매입 LOT를 같은 테이블에서 `source_type`으로 구분한다.
-   회사·상품·최초 입고 예정창고를 연결하고 공급자 LOT번호·BL·컨테이너·축산물
    이력번호·원산지·EST·생산일·소비기한을 관리한다.
-   원가는 원/KG 기준 LOT별 개별원가로 관리하고 평균원가는 사용하지 않는다.
    가격·개별원가는 정수 원 단위로 표시하며 소수점 이하는 올림한다.
-   동일 BL의 복수 컨테이너, 같은 컨테이너의 복수 상품·이력번호는 LOT를
    분리한다.
-   LOT 상태는 사용중·보류·마감으로 구분하며 사용중지는 물리삭제하지 않는다.
-   입고 BOX·KG 및 현재고는 후속 입고/출고 Transaction으로 관리한다.
-   창고이동 후 실제 재고위치는 LOT의 창고값을 덮어쓰지 않고 Transaction으로
    산출한다.

### 20.2 구현 파일

-   `models.py`: `Lot` ORM 추가
-   `db_lot_migrate.py`: `tb_lot` 비파괴 Migration 추가
-   `main.py`: 회사별 LOT 조회·생성·수정·사용중지·안전삭제 API 추가
-   `views/lot_reg.py`: LOT조회/보정 Pure Python PySide6 화면 추가
-   `views/mxmn_main_window.py`: 코드관리 `7.LOT조회/보정` 메뉴 연결

### 20.3 검증 결과

-   사용자 Windows 환경에서 LOT 화면 정상 표시 확인
-   Python 문법검사와 FastAPI LOT Route 로딩 통과
-   PySide6 Offscreen 화면 생성 및 버튼상태 검증 통과
-   LOT 수동 신규등록 제거
-   연결자료 없는 LOT만 삭제하고 연결자료가 있으면 DB FK로 차단
-   기존 상품·창고·거래처 기능 변경 없음

### 20.4 사용자 1차 실행검토 반영

LOT 화면 정상 표시를 확인한 뒤 업무구조를 다음과 같이 수정하였다.

-   LOT는 기초코드처럼 독립 신규등록하지 않는다.
-   `LOT입력` 화면을 `LOT조회/보정`으로 변경하고 수동 신규 버튼을 제거한다.
-   최초재고·매입입고·수입입고 저장 시 LOT가 원인 Transaction과 함께
    생성되도록 한다.
-   연결자료가 없는 임시/오입력 LOT에는 2회 확인 후 실제 삭제를 허용한다.
-   입고·출고·재고 등 외래키 연결자료가 있으면 DB 제약 오류를 API에서
    처리하여 물리삭제를 차단한다.
-   시스템 최초 도입자료는 `초기자료등록` 업무로 분리한다.
    - 최초재고: LOT 추적정보 + BOX + KG + 개별원가
    - 거래처별 미수·미지급 최초잔액
    - 통장/현금별 최초잔액
-   초기자료 입력 후 재고와 잔액은 매입·매출·입고·출고·이동·수금·지급
    Transaction의 논리적 결과로만 계산한다.

다음 구현 순서는 `초기자료등록 메뉴/기준일 → 최초재고 Header/Detail →
LOT+입고 원자적 생성 → 재고조회`이다. 거래처잔액과 금융잔액은 재고 원장
Vertical Slice 이후 각 원장 구조와 함께 연결한다.

### 20.5 수치 정밀도 확정

-   가격·단가·LOT 개별원가: 소수점 없이 원 단위, 소수점 이하는 올림
-   중량: KG 소수점 둘째 자리까지 (`256.37kg`)
-   BOX: 정수
-   환율: 소수점 둘째 자리까지

LOT 조회/보정 화면의 개별원가는 소수점 없는 정수로 변경했고, API에서도
입력값을 원 단위로 올림하여 저장한다. `tb_lot.individual_cost`의 기존
Scale은 호환성을 위해 변경하지 않고 Application Rule로 정수값을 보장한다.

### 다음 새 대화 시작문

> MXMN 개발을 GitHub `mixbiz1/MixNMenu`의 `main` 최신 소스와
> `docs/04_CURRENT_STATUS.md` 기준으로 이어서 진행합니다. 상품·창고·LOT
> 영역은 1차 완료되었습니다. 다음 단계로 `초기자료등록` 상위 메뉴를
> 설계하고, 최초재고와 거래처 최초잔액을 모두 지원해 주세요. 최초재고는
> 기준일·창고·상품·LOT 추적정보·BOX·KG·개별원가를 입력하여 LOT와 최초입고
> Transaction을 원자적으로 생성해야 합니다. 거래처 최초잔액은 미수금과
> 미지급금을 원거래 형태로 생성해 이후 부분수금·부분지급과 연결해야 합니다.
> 가격·단가·개별원가는 정수 원 단위로 소수점 올림, 중량은 소수점 둘째
> 자리, BOX는 정수, 환율은 소수점 둘째 자리 원칙을 적용해 주세요. 기존
> 정상 기능은 변경하지 말고 관련 문서·레거시 DB·모델·API·Migration·메뉴를
> 먼저 확인한 뒤 작은 Vertical Slice로 수정·검증·GitHub 동기화해 주세요.
